# dependencies
node_modules
.pnp
.pnp.js

# env (keep .env.example)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# next
.next
out

# service account (never commit)
proximity-learning-*.json
*-credentials.json

# misc
.DS_Store
*.pem
*.log
npm-debug.log*

# optional: uncomment if you don't want to commit the chunks (use env instead)
# data/chunks.json
